# minimum-c

A minimal `malloc` and `free` implementation in C using `sbrk`.

## Building

Requires CMake 4.1+ and a C17 compiler.

```sh
cmake -B build
cmake --build build
```

## Future

- `realloc` implementation
